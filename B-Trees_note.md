# 第18章 B树 学习笔记

---

## 一、为什么需要B树？

### 1.1 内存 vs 磁盘的巨大鸿沟

| 存储类型 | 访问时间 | 特点 |
|---------|---------|------|
| 主存（RAM） | ~50 纳秒 | 快，容量小，昂贵 |
| 磁盘（HDD/SSD） | ~4-8 毫秒 | 慢（约10万倍），容量大，便宜 |

**核心矛盾**：海量数据无法全部装入内存，必须存磁盘；但磁盘访问极慢，传统二叉搜索树（如红黑树）树高大（O(lg n)），每次搜索需 O(lg n) 次磁盘访问，性能不可接受。

### 1.2 B树的解决思路

**多路分支**：每个节点存储多个键，有成百上千个孩子（分支因子 50-2000），极大降低树高。

- 红黑树高度 ≈ 2 lg n ≈ 40（n=10^6）
- B树高度 ≈ log_t n ≈ 3（t=100, n=10^6）
- 例子：分支因子1001、高度2的B树可存储**超过10亿个键**，根节点常驻内存时只需至多2次磁盘访问。

### 1.3 磁盘访问模型

- 数据以**块**（block）为单位读写，典型大小 512~4096 字节
- B树节点大小 = 一个磁盘块，一次磁盘读写 = 一个B树节点
- `DISK-READ(x)`：将包含 x 的磁盘块读入内存
- `DISK-WRITE(x)`：将 x 的变化写回磁盘
- 运行时间由**磁盘访问次数**主导，CPU时间为次要因素

---

## 二、18.1 B树的定义

### 2.1 五个性质

B树 T 是一棵有根树，有**最小度数** t ≥ 2，满足：

**性质1 — 节点属性**：每个节点 x 包含
- `x.n`：当前键数
- `x.key₁ ≤ x.key₂ ≤ … ≤ x.key_{x.n}`：按单调非降排列的键
- `x.leaf`：TRUE（叶子）或 FALSE（内部节点）

**性质2 — 子指针**：内部节点还包含 `x.c₁, x.c₂, …, x.c_{x.n+1}`（x.n+1 个孩子）。叶子无子指针。

**性质3 — 键分离子树**：
```
k₁ ≤ x.key₁ ≤ k₂ ≤ x.key₂ ≤ … ≤ x.key_{x.n} ≤ k_{x.n+1}
```
其中 k_i 是子树 x.c_i 中的任意键。n 个键把区间分成 n+1 段，每段对应一个孩子。

**性质4 — 所有叶子同深度**：树完全平衡，高度记为 h。

**性质5 — 容量上下界**（核心）：
- **下界**：根以外每个节点至少有 **t−1** 个键，即至少 t 个孩子；根若非空至少有1键
- **上界**：每个节点最多 **2t−1** 个键，即最多 2t 个孩子
- **满节点（full）**：恰好有 2t−1 个键的节点
- t=2 时为 **2-3-4 树**（每个内部节点有 2、3 或 4 个孩子）

### 2.2 高度定理（Theorem 18.1）

> 若 n ≥ 1，则对于任意 n 键、高度 h、最小度数 t ≥ 2 的 B 树：
> ```
> h ≤ log_t ((n+1)/2)
> ```

**证明思路**：计算给定高度 h 下B树的**最少键数**：
- 根：1 键
- 深度 1：至少 2 个节点，每节点 t−1 键 → 2(t−1)
- 深度 i：至少 2t^(i−1) 个节点，每节点 t−1 键 → 2t^(i−1)(t−1)
- 总键数 n ≥ 1 + Σ 2(t−1)t^(i−1) = 2t^h − 1
- 整理得 h ≤ log_t((n+1)/2)

**意义**：B树高度 O(log_t n)，底数 t 远大于2，因此高度远低于红黑树。

---

## 三、18.2 基本操作：搜索、创建、插入

### 3.1 重要约定

1. **根节点常驻内存**：访问根不需要 DISK-READ，修改根需要 DISK-WRITE
2. **作为参数传入的节点已读入内存**
3. **所有操作都是"单次向下遍历"**（one-pass），绝不回溯

### 3.2 B-TREE-SEARCH：搜索

```
B-TREE-SEARCH(x, k)
1  i = 1
2  while i ≤ x.n and k > x.key_i       // 找到第一个 ≥ k 的位置
3      i = i + 1
4  if i ≤ x.n and k == x.key_i          // 找到了
5      return (x, i)
6  elseif x.leaf                        // 到叶子还没找到
7      return NIL
8  else DISK-READ(x.c_i)               // 读入对应子树
9      return B-TREE-SEARCH(x.c_i, k)
```

**复杂度**：磁盘 O(h)，CPU O(th) = O(t log_t n)

### 3.3 B-TREE-CREATE：创建空树

```
B-TREE-CREATE(T)
1  x = ALLOCATE-NODE()
2  x.leaf = TRUE; x.n = 0
3  DISK-WRITE(x)
4  T.root = x
```

O(1) 磁盘和CPU。

### 3.4 插入的核心思想：预分裂

> **在下行过程中遇到满节点就提前分裂，保证真正插入时父节点一定不满。**

### 3.5 B-TREE-SPLIT-CHILD：分裂满的孩子

给定非满父节点 x 和满孩子 y = x.c_i（有 2t−1 键）：

- y 的中间键 `key_t` **上升**到 x 的位置 i
- y 保留左边 t−1 键（+ t 个孩子）
- 新节点 z 获得右边 t−1 键（+ t 个孩子）
- z 插入为 x 的第 i+1 个孩子

```
分裂前 x:  ... N  W ...
            |
           y: P Q R S T U V (满, 7键)

分裂后 x:  ... N  S  W ...
            |     |
           y: P Q R  z: T U V
```

**关键代码**（LEFT-ROTATE类比）：
```
y.size = t-1                     // y缩减
z.n = t-1                        // z获得右半
x.key_i = y.key_t                // 中间键上升
x.c_{i+1} = z                    // z成为新孩子
x.n = x.n + 1
```

**复杂度**：CPU Θ(t)，磁盘 O(1)

### 3.6 B-TREE-SPLIT-ROOT：分裂根节点

根满了 → 创建新空根 s，旧根成为 s 的第一个孩子，然后 SPLIT-CHILD(s, 1) 分裂旧根。

**这是B树增加高度的唯一方式**——从顶上长高（vs 二叉搜索树从底下）。

### 3.7 B-TREE-INSERT：主过程

```
B-TREE-INSERT(T, k)
1  r = T.root
2  if r.n == 2t − 1              // 根满 → 先分裂根
3      s = B-TREE-SPLIT-ROOT(T)
4      B-TREE-INSERT-NONFULL(s, k)
5  else B-TREE-INSERT-NONFULL(r, k)
```

### 3.8 B-TREE-INSERT-NONFULL：核心递归

```
B-TREE-INSERT-NONFULL(x, k)
1  i = x.n
2  if x.leaf                       // 叶子：直接插入（数组右移）
3      while i ≥ 1 and k < x.key_i
4          x.key_{i+1} = x.key_i;  i = i − 1
5      x.key_{i+1} = k;  x.n = x.n + 1
6      DISK-WRITE(x)
7  else                            // 内部：找子树，预分裂，递归
8      while i ≥ 1 and k < x.key_i
9          i = i − 1
10     i = i + 1
11     DISK-READ(x.c_i)
12     if x.c_i.n == 2t − 1        // 孩子满？先分裂！
13         B-TREE-SPLIT-CHILD(x, i)
14         if k > x.key_i: i = i + 1  // 确定去哪个孩子
15     B-TREE-INSERT-NONFULL(x.c_i, k)
```

**复杂度总结**：磁盘 O(h)，CPU O(th) = O(t log_t n)

---

## 四、18.3 删除

### 4.1 删除的挑战

- 插入怕节点**太满** → 分裂
- 删除怕节点**太空**（< t−1） → 借键或合并
- 删除可能发生在任何节点（不限于叶子）
- 删除内部节点的键需要重组子树

### 4.2 核心策略：预借/预合并

> 递归向下时，保证目标子节点的键数 **≥ t**（比最小值多1），这样删除后就无需回溯修复。

**递归不变式**：每次调用 B-TREE-DELETE(x, k) 时，节点 x 键数 ≥ t（除非 x 是根）。

### 4.3 三种大情况

#### 情况1：到达叶子节点 x
- 若 k 在 x 中 → 直接删除，数组左移，x.n--
- 若 k 不在 x 中 → k 不在树中，无操作

#### 情况2：在内部节点 x 中找到 k（k = x.key_i）

| 子情况 | 条件 | 操作 |
|--------|------|------|
| 2a | x.c_i 键数 ≥ t | 找到 k 的**前驱** k'（左子树最大值），递归删 k'，用 k' 替换 k |
| 2b | x.c_i 只有 t−1，但 x.c_{i+1} ≥ t | 找到 k 的**后继** k'（右子树最小值），递归删 k'，用 k' 替换 k |
| 2c | 两个子节点都只有 t−1 | **合并**：k + x.c_i + x.c_{i+1} → 一个满节点(2t−1键)；从 x 中移除 k 和 x.c_{i+1}；在合并节点中递归删 k |

#### 情况3：在内部节点 x 中未找到 k → 向下递归到 x.c_i

**进入前保证 x.c_i 有 ≥ t 键**：

| 子情况 | 条件 | 操作 |
|--------|------|------|
| 3a | x.c_i 的左/右兄弟 ≥ t 键 | **借键（旋转）**：父键下移给 x.c_i，兄弟一个键上移给父，兄弟对应孩子移到 x.c_i |
| 3b | 所有相邻兄弟都只有 t−1 | **合并**：x.c_i + 兄弟 + 父键(x.key) → 一个满节点；从 x 移除键和孩子；在合并节点中继续查找 |

### 4.4 根变空与树高降低

当情况2c或3b导致根键数变为0时：
- 删除旧根
- 将合并后的大节点设为新根
- **树高减1**（与插入时根分裂、树高加1对称）

### 4.5 借键（旋转）示意图

```
从左兄弟借键：
借前:   ... key_{i-1} ...
        /             \
    [... X Y]      [A ...]       (右子只有A, 键数=t-1)
    键数≥t

借后:   ... X ...
        /         \
    [... Y]      [key_{i-1} A ...]
    键数-1        键数+1

父键key_{i-1}下移给右子，左兄弟最大键X上移给父
```

### 4.6 复杂度

- **磁盘访问**：O(h)，每层 O(1) 次读写
- **CPU时间**：O(th) = O(t log_t n)

---

## 五、关键公式与对比总结

| 项目 | 红黑树 | B树 |
|------|--------|-----|
| 分支因子 | 2 | 2 ~ 2t（t≈50~2000） |
| 高度 | ≤ 2 lg n | ≤ log_t((n+1)/2) |
| 搜索磁盘访问 | O(lg n) | O(log_t n) |
| 插入磁盘访问 | O(lg n) | O(log_t n) |
| 删除磁盘访问 | O(lg n) | O(log_t n) |
| 节点存储 | 1键 + 颜色 | t-1 ~ 2t-1 键 |
| 增高方式 | 底部变色/旋转 | 顶部根分裂 |
| 降低方式 | 底部旋转 | 顶部根合并/删除 |

---

## 六、重要练习思路

### 18.1-1：为何 t=1 不允许？
- 非根节点至少 0 键（无意义），最多 1 键
- 无法形成有效的平衡多叉结构
- t 必须 ≥ 2，保证每个非根节点至少 1 键

### 18.1-2：图18.1合法t值
- 观察到节点最少1键，最多3键
- 需要 t−1 ≤ 1 且 2t−1 ≥ 3
- 解得 t = 2（唯一解）

### 18.2-6：节点内二分查找
- 每个节点查找时间：O(lg t)
- 总CPU：O(h × lg t) = O(lg n)
- 与 t 的选取无关！

### 18.2-7：最优t值选择
- 磁盘访问时间 = a + bt
- 总搜索时间 ∝ (a+bt) × h ≈ (a+bt)/(lg t)
- 求导找最优 t ≈ a/b 数量级
- a=5ms, b=10μs → t 约几百

---

## 七、学习要点

1. **B树的价值在于减少磁盘访问**，每次访问一个整块（一个节点），用空间换时间
2. **t 是最关键的参数**，决定了分支因子和树高
3. **插入和删除都采用"预防性单次向下遍历"**，在路上遇到问题当场解决
4. **分裂（满→半满）** 和 **借键/合并（太空→补足）** 是维持平衡的两种基本操作
5. 树高增减只在顶部发生（根分裂/根删除），保证完全平衡
6. 所有操作 O(h) 磁盘访问，O(th) CPU